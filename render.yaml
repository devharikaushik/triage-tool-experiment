services:
  - type: web
    name: med-dev-vi
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: DATABASE_URL
        value: sqlite:///./med_dev_vi.db
      - key: ALLOWED_HOSTS
        value: med-dev-vi.onrender.com,*.onrender.com
